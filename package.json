{
  "name": "tp-final",
  "version": "1.0.0",
  "description": "paquete de practico final",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "@types/prompt-sync": "^4.2.3",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "prompt-sync": "^4.2.0"
  }
}
